-- Grade Arena — AI-generated notes for uploaded files
-- Run this once in the Supabase SQL Editor against a project that already
-- has migration.sql applied.

alter table public.files add column ai_notes text;
